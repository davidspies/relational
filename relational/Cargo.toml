[package]
name = "relational"
edition.workspace = true
version.workspace = true

[features]
consolidate_all = []

[dependencies]
anyhow = "*"
contiguous_data = { path = "../contiguous_data" }
derive-where = "*"

[dev-dependencies]
proptest = "*"
