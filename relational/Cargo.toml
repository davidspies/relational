[package]
name = "relational"
edition.workspace = true
version.workspace = true

[features]
consolidate_all = []

[dependencies]
derive-where = "*"
anyhow = "*"
contiguous_data = { path = "../contiguous_data" }

[dev-dependencies]
proptest = "*"
