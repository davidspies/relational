[package]
name = "relational"
edition.workspace = true
version.workspace = true

[features]
consolidate_all = []

[dependencies]
derive-where = "*"
anyhow = "*"
index_heap = { path = "../index_heap" }

[dev-dependencies]
proptest = "*"
