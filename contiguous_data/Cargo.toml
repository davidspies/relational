[package]
name = "contiguous_data"
edition.workspace = true
version.workspace = true

[features]
default = []
std-hash = []

[dependencies]
rustc-hash = { workspace = true }
arrayvec = "*"
derive-where = "*"
index_list = { version = "*" }

[dev-dependencies]
proptest = "*"
