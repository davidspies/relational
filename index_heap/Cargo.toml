[package]
name = "index_heap"
edition.workspace = true
version.workspace = true

[dependencies]
index_list = { version = "*", features = ["iter_mut"] }
arrayvec = "*"

[dev-dependencies]
proptest = "*"
