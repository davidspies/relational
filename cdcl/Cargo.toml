[package]
name = "cdcl"
edition.workspace = true
version.workspace = true

[features]
dhat-heap = ["dep:dhat"]

[dependencies]
ahash = "*"
dhat = { version = "*", optional = true }
anyhow = "*"
clap = { version = "*", features = ["derive"] }
consume_on_drop = "*"
contiguous_data = { path = "../contiguous_data" }
ctrlc = "*"
priority-queue = "*"
rand = "*"
rand_chacha = "*"
relational = { path = "../relational" }
